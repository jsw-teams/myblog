---
import { readSiteConfig } from "../lib/content.mjs";
import { renderNotFoundPage } from "../templates.mjs";

const site = await readSiteConfig();
const html = renderNotFoundPage({ site });
---
<Fragment set:html={html} />
